<nav class="nav">
  <div class="nav-inner">
    <a href="/" class="nav-logo" style="text-decoration:none">AXIO <span>MALL</span></a>
    <div class="nav-links">
      <a href="/shop">상품목록</a>
      <% if (user) { %>
      <a href="/shop/cart">🛒 장바구니</a>
      <a href="/shop/orders">주문내역</a>
      <% } %>
      <a href="/challenges">챌린지</a>
      <a href="/" style="color:var(--text2);font-size:12px">← 허브</a>
      <div class="nav-user">
        <% if (user) { %>
        <span class="badge <%= user.role === 'admin' ? 'admin' : '' %>"><%= user.username %></span>
        <a href="/logout" class="btn btn-secondary btn-sm">로그아웃</a>
        <% } else { %>
        <a href="/login" class="btn btn-primary btn-sm">로그인</a>
        <% } %>
      </div>
    </div>
    <button class="hamburger" id="hamburger" aria-label="메뉴">
      <span></span><span></span><span></span>
    </button>
  </div>
  <div class="mobile-nav" id="mobileNav">
    <a href="/shop">상품목록</a>
    <% if (user) { %>
    <a href="/shop/cart">🛒 장바구니</a>
    <a href="/shop/orders">주문내역</a>
    <% } %>
    <a href="/challenges">챌린지 🚩</a>
    <a href="/">← 메인 허브</a>
    <% if (user) { %>
    <div class="mobile-user">
      <span><%= user.username %></span> ·
      <a href="/logout">로그아웃</a>
    </div>
    <% } else { %>
    <a href="/login">로그인</a>
    <% } %>
  </div>
</nav>
<div id="toast-container"></div>
<script src="/js/main.js"></script>
